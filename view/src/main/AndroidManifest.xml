<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.view">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">

        <activity android:name=".ListView.NormalListView.NormalListtViewActivity"/>
        <activity android:name=".ListView.OptimizationListView.OptimizationListtViewActivity"/>
        <activity android:name=".ListView.LoadListView.LoadListViewActivity"/>
    </application>

</manifest>
